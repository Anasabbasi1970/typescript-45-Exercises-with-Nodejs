// Crating a Array
let petAnimals = ["Cat" , "Dog", "Rabbit"]

// Using For-Loop
for(let onepet of petAnimals){
    console.log(`A ${onepet} would make a grat pet.`)
}

// Print a Message outside of For-Loop

console.log("Any of these animals would make a grat pet!")
