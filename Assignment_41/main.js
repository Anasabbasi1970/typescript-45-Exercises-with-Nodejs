// Define a function to print each magnician name from an array
function show_magicians(magnicians) {
    magnicians.forEach(function (name) { return console.log(name); });
}
// Define an array containing magicians name
var magician_names = ["Ali", "Anas", "Usman"];
// call the function to Print each magician name
show_magicians(magician_names);
